/** EU — parametric chlorine & turbidity dominate the composite. */
window.EuBenchmarkWeights = Object.freeze({
  chlorine: 0.25,
  turbidity: 0.25,
  tds: 0.15,
  ph: 0.15,
  do: 0.10,
  orp: 0.10
});
