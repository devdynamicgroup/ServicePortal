/** US EPA — turbidity (TT-like) weighted highest; chlorine upper bound lenient. */
window.UsEpaBenchmarkWeights = Object.freeze({
  turbidity: 0.30,
  tds: 0.20,
  chlorine: 0.15,
  ph: 0.15,
  do: 0.10,
  orp: 0.10
});
