/** Thailand benchmark — equal weights among scored local parameters (DO/Temp excluded). */
window.ThailandBenchmarkWeights = Object.freeze({
  ph: 1,
  tds: 1,
  chlorine: 1,
  turbidity: 1,
  orp: 1
  // do / temp intentionally omitted — Thailand does not score them
});
