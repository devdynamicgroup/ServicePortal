# M8.8 — Implementation Plan (Rollout Operations)

**Status:** Ops package implemented (flags remain OFF; enablement is human-gated)  
**Mode:** Ops artifacts landed — runbook / checklist / rollback / read-only gate script  
**Architecture:** `docs/M8.8_ROLLOUT_ENABLEMENT_PLAN.md` (approved with changes)  
**Out of scope:** Enabling production flags in-repo; M8.9; Case LINE removal; auto-merge; ownership moves

---

## Locked decisions (from READY WITH CHANGES)

| Decision | Lock |
|----------|------|
| Soak — dual-write (Phase 2) | Staging ≥ **48h**; Production ≥ **72h** before Phase 3 |
| Soak — shadow (Phases 3 & 5) | Min **48h** **and** volume gate (LINE ≥ **200** lookups; Notify ≥ **100** resolves) **or** ≥ **7 days** if volume low |
| Soak — primary (Phases 4 & 6) | ≥ **72h** before next phase |
| Phase order | **Phase 4 required before Phase 5** (no waiver without written ops sign-off) |
| LINE diverge pre-read | Phase 3 blocked if any **unticketed** `lineDiverge`; ticketed diverges allowed with owner + no auto-repair |
| Rollback | Flag-only; reverse order; no data rewrite |

---

## Goal

Deliver **operable rollout packaging** so humans can run Phases 0–6 safely:

1. Locked gate thresholds  
2. Phase runbooks + go/no-go checklist  
3. Monitoring cheat-sheet (commands + log markers)  
4. Flag-only rollback card  

M8.8 does **not** change runtime readers. Enabling flags is an **ops deploy action**, not an application code change.

---

## Scope boundary

### In scope (implementation of M8.8 ops package)

| Deliverable | Form |
|-------------|------|
| Phase runbook | Doc + copy/paste checklist |
| Gate command sheet | Reconcile / merge detect / metrics dump / tests |
| Monitoring sheet | Metrics + log events + soak clocks |
| Rollback card | Exact env flag order |
| Optional: `scripts/check-customer-rollout-gates.js` | **Read-only** — prints reconcile latest summary + flag snapshot; **never sets env flags** |

### Out of scope

- Setting any `CUSTOMER_DOMAIN_*` to true in repo defaults / production  
- Changing `line-reader` / `notify-reader` / dual-write / merge behavior  
- HTTP endpoints that mutate flags or repair  
- Removing Case LINE fields  
- M8.9  

---

## Files to create (when implementing this plan)

| File | Responsibility |
|------|----------------|
| `docs/M8.8_PHASE_RUNBOOK.md` | Phase 0–6 steps, success, monitor, rollback triggers |
| `docs/M8.8_GO_NO_GO_CHECKLIST.md` | Sign-off checklist (staging → prod) |
| `docs/M8.8_ROLLBACK_CARD.md` | One-page flag-only rollback |
| `scripts/check-customer-rollout-gates.js` *(optional)* | Read-only: flags OFF/ON snapshot + `latest.summary.json` gateResult; exit 0/2 |

### Files to modify

| File | Change |
|------|--------|
| `docs/PROJECT_STATE.md` | M8.8: ops package status; flags still OFF |
| `docs/ENVIRONMENT.md` | Link runbook; restate “do not enable until gates” |
| `docs/M8.8_ROLLOUT_ENABLEMENT_PLAN.md` | Status → implementation-ready / ops package landed |

**Do not modify:** `flags.js` defaults, workflow notify path, line-routes, Offer/Workflow/Feedback/Report modules.

---

## Runtime (unchanged until ops enables)

```text
CUSTOMER_DOMAIN_ENABLED=false
CUSTOMER_DOMAIN_DUAL_WRITE=false
CUSTOMER_DOMAIN_READ_LINE=false
CUSTOMER_DOMAIN_READ_LINE_SHADOW=false
CUSTOMER_DOMAIN_READ_NOTIFY=false
CUSTOMER_DOMAIN_READ_NOTIFY_SHADOW=false
CUSTOMER_DOMAIN_MERGE_ENABLED=false
```

---

## Phase package (summary)

| Phase | Enable | Hold | Go criteria | Rollback |
|-------|--------|------|-------------|----------|
| 0 | none | — | Reconcile archived; tests green | — |
| 1 | ENABLED | ≥24h | Health + Customers DB ready | ENABLED=false |
| 2 | DUAL_WRITE | ≥72h prod | Dual-write ≥99%; Case unblocked | DUAL_WRITE=false |
| 3 | READ_LINE_SHADOW | ≥48h + volume/7d | mismatch &lt;1%; conflicts 0; diverge ticketed | SHADOW=false |
| 4 | READ_LINE | ≥72h | fallback OK; mismatch &lt;0.5% | READ_LINE=false |
| 5 | READ_NOTIFY_SHADOW | ≥48h + volume/7d | mismatch &lt;1%; wrong-recipient 0 | SHADOW=false |
| 6 | READ_NOTIFY | ongoing | send ≥ baseline; Case wins diverge | READ_NOTIFY=false |

Full detail remains in `docs/M8.8_ROLLOUT_ENABLEMENT_PLAN.md`.

---

## Gate commands (operators)

```bash
# Pre-phase / between phases
node scripts/test-customer-line-read.js
node scripts/test-customer-notify-read.js
node scripts/test-customer-reconcile.js
node scripts/run-customer-reconcile.js scan
node scripts/run-customer-merge.js detect
node scripts/run-customer-reconcile.js status
node scripts/dump-customer-line-read-metrics.js
node scripts/dump-customer-notify-read-metrics.js
```

Archive `tmp/customer-reconcile/reconcile-*.summary.json` per environment per phase.

---

## Monitoring cheat-sheet

| Area | Source |
|------|--------|
| Flags | `run-customer-reconcile.js status` / ops health `customerDomain` |
| Reconcile | scan counters + `gateResult` |
| LINE read | `[customer_line_read]` logs + dump script |
| Notify read | `[customer_notify_read]` + `[line_close_notify]` |
| Dual-write | dual-write result status on create/link |

---

## Rollback card (flag-only)

```text
1. CUSTOMER_DOMAIN_READ_NOTIFY=false
2. CUSTOMER_DOMAIN_READ_NOTIFY_SHADOW=false
3. CUSTOMER_DOMAIN_READ_LINE=false
4. CUSTOMER_DOMAIN_READ_LINE_SHADOW=false
5. CUSTOMER_DOMAIN_DUAL_WRITE=false   # if write path unstable
6. CUSTOMER_DOMAIN_ENABLED=false     # if master switch unstable
```

Redeploy / restart as required by host. **No** customer delete, unlink, or Case rewrite.

---

## Testing plan (for ops package implementation)

| # | Check |
|---|--------|
| 1 | Docs list all 7 phases in fixed order |
| 2 | Locked soak/volume numbers match architecture approval |
| 3 | Phase 4 before Phase 5 stated as mandatory |
| 4 | Optional gate script never writes env or Notion |
| 5 | Repo flag defaults remain false after any doc/script land |
| 6 | No changes to line-reader / notify-reader behavior |

---

## Regression risks

| Risk | Mitigation |
|------|------------|
| Docs treated as “enable now” | Explicit: flags stay OFF until ops checklist signed |
| Optional script mutates state | Read-only; exit codes only |
| Skipping Phase 4 | Locked in runbook + checklist |

---

## Readiness

### READY

Locks applied; deliverables are documentation (+ optional read-only gate script). Safe to implement the ops package **without enabling production flags**.

Await approval to write the runbook/checklist/rollback docs (still **no flag enable**, **no M8.9**).
