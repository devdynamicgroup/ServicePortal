# M9.6 Track A — Launch Checklist & Checkpoint A Sign-off

**Status:** Operations readiness — awaiting human sign-off  
**Mode:** Ops only — **no code**, **no flag enablement by this document**, **no production SEND executed here**, **no M9.7**  
**Track:** A — execute first Care launch (`docs/M9.6_TRACK_DECISION.md`)  
**Canonical depth:** Use with `docs/M9.4_CARE_GO_NO_GO_CHECKLIST.md` · `docs/M9.4_CARE_FIRST_SEND_PLAN.md` · `docs/M9.4_CARE_PRODUCTION_RUNBOOK.md` · `docs/M9.4_CARE_ROLLBACK_CARD.md`  
**Evidence:** `docs/M9.6_CHECKPOINT_A_EVIDENCE_PACK.md` · **Sign-off:** `docs/M9.6_CHECKPOINT_A_SIGNOFF_RECORD.md`

---

## Purpose

Single ops front door for **Track A Checkpoint A**: confirm foundation, dry-run evidence, launch blockers, and **Operator + Reviewer + On-call** sign-off **before** any human sets:

```text
CARE_LIFECYCLE_ENABLED=true
CARE_LIFECYCLE_SEND=true
```

**Completing or filing this checklist does not set flags and does not send LINE.**

---

## Current state (baseline)

| Area | State |
|------|--------|
| M8.5–M8.9 Customer Identity | Foundation complete; flags OFF |
| M9.0–M9.5 Care | Runtime + ops + outcomes + CDR + rollout + steady-state complete |
| M9.6 | Track A selected; B/C/D deferred |
| Production Care messages | **None sent yet** |

### Flags (must remain false until after Checkpoint A GO + human apply)

```text
CARE_LIFECYCLE_ENABLED=false
CARE_LIFECYCLE_SEND=false
CARE_OUTCOME_TRACKING=false
CARE_OUTCOME_REPORT=false

CUSTOMER_DOMAIN_* = false
```

Outcome flags may stay OFF through first SEND (file reports OK). Customer Domain not required for Care launch.

---

## Ownership (unchanged)

| Domain | Owns |
|--------|------|
| Case | Ops SSOT; result notification state |
| Customer | Identity only |
| Care | Policy, events, audits, outcomes, CDRs |

---

## Pre-flight commands (observe only)

```bash
node scripts/test-care-lifecycle.js
node scripts/test-care-outcomes.js
node scripts/test-care-governance.js
node scripts/test-care-production-readiness.js
node scripts/test-care-steady-state.js
node scripts/check-care-rollout-gates.js
node scripts/check-care-production-readiness.js
node scripts/check-care-steady-state.js
node scripts/check-care-patterns.js
node scripts/run-care-outcome-report.js
node scripts/run-care-lifecycle.js scan --event=reinspection_6mo --mode=dry-run
```

Archive dry-runs under `tmp/care-lifecycle/`.

---

## Section 1 — Environment & roles

- [ ] Environment: _______________ (staging / production)  
- [ ] Staging dry-run completed (if staging exists) / N/A: _______________  
- [ ] Operator: _______________  
- [ ] Reviewer: _______________  
- [ ] On-call: _______________  
- [ ] Date: _______________  
- [ ] On-call has `docs/M9.4_CARE_ROLLBACK_CARD.md` open  
- [ ] Confirmed: this checklist will **not** be used to auto-set env flags  

---

## Section 2 — Foundation gates

- [ ] Care fixture tests green (lifecycle / outcomes / governance / production-readiness / steady-state as applicable)  
- [ ] Case result-send path healthy (baseline)  
- [ ] Care Audit separate from Case `notificationStatus`  
- [ ] Only `care.reinspection_6mo` (no new campaigns; Track B/C/D not in scope)  
- [ ] `CUSTOMER_DOMAIN_*` remain false (not required)  
- [ ] `CARE_OUTCOME_*` may remain false  
- [ ] Single cron/runner plan documented for post-SEND  
- [ ] Care copy / Thai care tone approved  

---

## Section 3 — Dry-run validation (required before Checkpoint A)

- [ ] ≥ **3** successful dry-run scans archived  
- [ ] Window ≥ **7 days** (≥ **14 days** if low volume)  
- [ ] Spot-check ≥ **20** `dry_run` rows (or all): anchors OK, LINE present or `no_line` explained  
- [ ] Skip reasons explained (`too_recent`, `consent_line_false`, `no_line`, `missing_anchor`, `already_sent`, …)  
- [ ] Unticketed false positives = **0**  
- [ ] Idempotent re-scan does not double-plan  
- [ ] Reports show Case notification **unchanged**  
- [ ] Pattern / production-readiness warnings triaged  

Dry-run command:

```bash
node scripts/run-care-lifecycle.js scan --event=reinspection_6mo --mode=dry-run
```

---

## Section 4 — Launch blockers (all must be N)

| Blocker | Y/N |
|---------|-----|
| Wrong Case anchors in sample | |
| Care writes Case `notificationStatus` | |
| Unexplained eligibility / volume jump | |
| Consent skips ignored | |
| No single-runner plan | |
| On-call unfamiliar with rollback | |
| Fixture / gate scripts failing | |
| Plan to skip limit ladder or observe windows | |
| Plan to enable Track B/C/D in same launch | |

---

## Section 5 — Post–Checkpoint A plan (acknowledge only)

Operators acknowledge the ladder; **do not execute SEND in this sign-off turn** unless separately authorized after GO:

```text
Human sets ENABLED=true (if not already), then SEND=true
  → --limit=10 → observe ≥24h → Checkpoint B
  → --limit=50 → observe ≥48h (prefer 72h) → Checkpoint C
  → uncapped → M9.5 steady state
```

- [ ] Fail-pause understood: `failed/(sent+failed) ≥ 0.20` with n≥5 → `SEND=false`  
- [ ] Rollback order known: SEND off first, then ENABLED if needed  
- [ ] Checkpoints B/C = Operator + Reviewer (On-call if incident)  
- [ ] Full detail: `docs/M9.4_CARE_FIRST_SEND_PLAN.md`  

---

## Checkpoint A — Sign-off

**Requires:** Operator + Reviewer + On-call  
**Record:** Prefer formal signatures on `docs/M9.6_CHECKPOINT_A_SIGNOFF_RECORD.md` after evidence pack is complete.  

**Meaning:** Authorization for humans to enable SEND and start `--limit=10` when they choose — **not** an automatic flag write.

- [ ] Evidence pack complete (`docs/M9.6_CHECKPOINT_A_EVIDENCE_PACK.md`)  
- [ ] Sections 1–4 of this checklist complete; all blockers **N**  
- [ ] Sign-off record filled (`docs/M9.6_CHECKPOINT_A_SIGNOFF_RECORD.md`)  
- [ ] **GO** to proceed to human flag enable + first send / **NO-GO**: _______________  

| Role | Name | Date | GO / NO-GO | Signature / ack |
|------|------|------|------------|-----------------|
| Operator | | | | |
| Reviewer | | | | |
| On-call | | | | |

### After GO (human actions — outside this doc’s auto scope)

1. Set `CARE_LIFECYCLE_ENABLED=true` (if still false)  
2. Set `CARE_LIFECYCLE_SEND=true`  
3. Run first send per first-send plan (`--limit=10`)  
4. Observe ≥24h before Checkpoint B  

### After NO-GO

- Leave all CARE flags **false** (or ENABLED-only if already in Phase 1 observe)  
- Document gaps; re-run dry-run; do **not** start Tracks B/C/D  

---

## Explicit non-actions (this milestone / this file)

- Do **not** change `services/care-lifecycle/**`  
- Do **not** enable flags from scripts or agents “because checklist exists”  
- Do **not** send production Care messages as part of creating/updating this doc  
- Do **not** enable `CUSTOMER_DOMAIN_*` for Care  
- Do **not** start M9.7 or Tracks B/C/D  

---

## Record

| Field | Value |
|-------|-------|
| Checklist version | M9.6 Track A / Checkpoint A |
| Filled by | |
| Stored copy path | (e.g. ops drive / `docs/care-decisions/`) |
| Linked dry-run runIds | |
| Linked readiness JSON date | |
