# Quality V3 Evidence Framework

This directory is the evidence boundary for Quality V3 (`src/js/score/production/computeQualityScoreV2.js`).
It exists to make it structurally hard to repeat the Case-1328 overfitting
pattern: calibrating a model against one sample, then citing the same
sample's post-hoc test assertions as if they were independent validation.

**Nothing in this directory changes model behavior.** It is pure
documentation and data-governance tooling (`scripts/quality-v3/evidenceRegistry.js`
has zero dependency on any scoring engine — see the accompanying test suite).

## Contents

- [`evidence-registry/`](evidence-registry/) — **constant/clause** evidence registry (red-flag hunt baseline for Product Decision). Machine-readable [`constants.json`](evidence-registry/constants.json). Model remains frozen; `NO CITATION` is an explicit status, not a blank.
- [`evidence-registry.json`](evidence-registry.json) — machine-readable record of every known **measurement sample** (real or synthetic) and what it has/hasn't been used for. Distinct from constant/clause registry above.
- [`CASE_1328_RECLASSIFICATION.md`](CASE_1328_RECLASSIFICATION.md) — why Case 1328 is calibration data, not validation evidence, and cannot become holdout evidence.
- [`MODEL_PROVENANCE.md`](MODEL_PROVENANCE.md) — the authoritative record of what evidence Quality V3 `quality-v3.0` currently rests on.
- [`PARAMETER_EVIDENCE_MATRIX.md`](PARAMETER_EVIDENCE_MATRIX.md) — per-parameter (pH/TDS/turbidity/ORP/chlorine/DO) evidence classification.
- [`CALIBRATION_WORKFLOW.md`](CALIBRATION_WORKFLOW.md) — the four evidence categories, the partition rule, the anti-pattern this framework exists to prevent, and the required future workflow.
- [`UNRESOLVED_DECISIONS.md`](UNRESOLVED_DECISIONS.md) — **Product Decision Log**. **All five PDs DECIDED A**: PD-005 (forbid magnitude ranking), PD-001 (pass-band presentation), PD-002 (EU gate 65 = project hard gate / UNSUPPORTED ANCHOR), PD-003 (Thailand DO excluded = project design), PD-004 (ORP 200–600 = shared operational / project band). Model/Score frozen.
- [`EVIDENCE_ACQUISITION_PROTOCOL.md`](EVIDENCE_ACQUISITION_PROTOCOL.md) — the required process for adding new real samples to the registry, including the novelty check that blocks re-using known fixture values as if they were new evidence.
- [`SAMPLE_INTAKE_TEMPLATE.json`](SAMPLE_INTAKE_TEMPLATE.json) — the intake form for a new candidate sample.
- [`DECISION_MATRIX.md`](DECISION_MATRIX.md) — every open decision point in one table, plus two empirical (not decision-making) findings: aggregation-sensitivity numbers for Case A/B under alternative aggregation formulas, and a country-plateau audit of how wide the flat-100 region is inside each engine's own PASS window. Selects nothing; still requires human sign-off per `CALIBRATION_WORKFLOW.md` §5.
- [`SCORING_DIAGNOSTIC_REPORT.md`](SCORING_DIAGNOSTIC_REPORT.md) — full read-only diagnostic: dense curve tables verified against live code (incl. two newly-found cliffs — Quality chlorine at 0.08 mg/L, EU country-benchmark chlorine at its band edges), Case A/B contribution breakdown, per-country boundary probe, and a proven/uncertain/frozen/needed summary. Confirms no production code, model, or Case data was changed.
- [`EVIDENCE_BASED_SCORING_AUDIT.md`](EVIDENCE_BASED_SCORING_AUDIT.md) — external WHO/regulatory research per parameter, full mechanism explanation for the chlorine cliff (an unintentional branch collision, vs. the EU engine's intentional hard-safety-gate cliff), and per-parameter GREEN/YELLOW/RED decision gates. Finds two math errors in the code's own documented rationale (pH and Chlorine "midpoint" comments are numerically wrong). Proposes zero replacement breakpoints — evidence found supports band/direction only, never a specific new number.
- [`CANDIDATE_SCORING_DESIGN_REVIEW.md`](CANDIDATE_SCORING_DESIGN_REVIEW.md) — takes the audit one step further: multiple labeled candidate options (A/B/C...) per parameter, each with source, rationale, and an honest confidence rating (including "NONE" where nothing defensible exists), for a human to review and select from. Selects nothing itself.
- [`FINAL_SCORING_IMPLEMENTATION_REPORT.md`](FINAL_SCORING_IMPLEMENTATION_REPORT.md) — **the two approved changes are now implemented in `computeQualityScoreV2.js` (working tree only, not committed/deployed).** Full diff, before/after tables verified against the actual edited file, country isolation re-confirmed (184 pre-existing repo tests run: 182 pass, 2 fail). The 2 failures are a pre-existing test (`quality-v2-calibration.test.js` lines 168-169) asserting chlorine 0.2/0.5 must score below 0.3 — exactly the old, non-evidence-backed assumption this review series concluded was false precision. Not silently fixed; flagged as an open decision. Awaiting review before commit.
- [`REALITY_FIRST_IMPLEMENTATION_REPORT.md`](REALITY_FIRST_IMPLEMENTATION_REPORT.md) — pre-implementation validation for exactly the two approved changes (Turbidity center, Chlorine continuity fix): before/after table, per-parameter attribution of every score movement, full regression suite (monotonicity, no inversion, country isolation — all PASS), and an explicit flag that the chlorine fix's magnitude is larger in practice than "remove the cliff" suggests (the old curve's non-evidenced branches were steeper throughout, not just discontinuous at one point). Country benchmark columns confirmed byte-identical before/after, both architecturally (zero shared code, verified by grep) and empirically. No file under `src/` touched.
- [`REALITY_FIRST_SCORING_REVIEW.md`](REALITY_FIRST_SCORING_REVIEW.md) — the most decisive pass: names two changes as ready-to-implement today (Turbidity center, Chlorine cliff fix — both pass every false-precision/monotonicity check cleanly) and gives each of 9 open items an explicit *recommended* option with rationale, while still requiring approval for all of them. Separates the "conservative" candidate (Turbidity+Chlorine cliff only: Case A unchanged at 92, Case B 73→78) from the fuller exploratory one in `CANDIDATE_SCORING_TABLE.md` (Case A 92→95) so the two are never confused. Names 4 explicit false-precision risks — places where *more* granularity would make the model less trustworthy, not more.
- [`CANDIDATE_SCORING_TABLE.md`](CANDIDATE_SCORING_TABLE.md) — a single, internally-consistent candidate Quality curve set (isolated diagnostic script, never touches production), with every breakpoint classified by evidence type. Headline finding: evidence-alignment is **not the same as more resolution** — the evidence-backed pH candidate is flatter than current (WHO: no graded guideline warranted), and it raises every non-ideal fixture's score (Case A 92→95, Case B 73→84, LOCKED 71→83, POOR 39→55) since WHO's real bands are wider than this codebase's invented ones. Only Turbidity and the Chlorine cliff genuinely improve; ORP and DO get no candidate at all.
- [`COUNTRY_SCORE_SEMANTICS_REVIEW.md`](COUNTRY_SCORE_SEMANTICS_REVIEW.md) — the actual product-definition gap behind "TH=100=JP=100", **now confirmed across all 5 engines** (not just Thailand/Japan): every country engine's own `verdictFrom()` uses a 5-tier quality-gradient label (Excellent/Good/Acceptable/Attention/Poor) that implies more resolution than its flat-in-band grading curve delivers — a self-inconsistency in the *existing code*, not a bug (WHO's TDS/Turbidity are the sole partial exception). Traces `classifications` (binary, correct) vs `score`/`verdict` (ambiguous intent) as two independent, parallel outputs per engine; includes a full blast-radius dependency map and EVIDENCE/CODE-FACT/DECISION/CONSEQUENCE breakdown per option. ORP kept on its own separate track per instruction — not used as evidence for this decision. **Update: now includes a specific recommendation** (Country Score = Compliance Score) grounded in the project's own architecture comment (`computeQualityScoreV2.js` lines 1-13: "Benchmark Comparison" is listed as a peer of "Compliance," separate from "Quality Score 0-100," which Quality V3 already owns) — the strongest original-intent evidence found in the repo for this decision. TH=100/JP=100 confirmed NOT a bug under this semantic; remaining gap reclassified as a communication/labeling question, not a scoring one. Final approval still requires product owner sign-off — this document argues for one answer with evidence rather than presenting 3 options neutrally. **Update: now also includes a Realism/Overfitting Audit** — 218-point wide sweep (pH 5-10, TDS 0-2200, Turbidity 0-16, Chlorine 0-5.5, ORP 0-800, DO 0-12) found zero monotonicity violations (model generalizes correctly on never-seen inputs) but confirmed via git history that all 5 engines' numeric anchors trace to one founding commit validated against a single sample (`docs/BENCHMARK_ENGINE_COMPARISON_SAMPLE.md`, "Spread: 35 points"), not cited external standards. EU's `gateCapOnChlorineFail: 65` specifically has zero provenance and causes 23.4% of the sweep to collapse to exactly that value. No fix proposed — STOP condition invoked per instruction (no evidence to derive a replacement number). **Round 2 addendum**: full weights/hard-gate provenance table (every weight across all 5 engines is a stated design *philosophy* with an unsupported specific *magnitude*); calibration-vs-hold-out check (218+ never-tuned points behave identically to the original fixtures — mechanism generalizes); 6 deliberately-designed cross-country stress profiles computed blind (including one transparently-reported wrong prediction, corrected by the computed result, not hidden); explicit Case A-F classification (B+C-mild+E apply, D not found). Still no fix implemented — "NO SAFE FIX YET." **Final pass**: complete ~90-constant inventory with exact `file:line` for every band, slope, weight, and the one hard gate across all 5 engines, strictly classified A-E. Result: only 3 constants trace to real evidence (WHO's TDS tiers, US EPA's TDS SMCL — independently verified as a real EPA standard, and EU's chlorine band via an already-cited Directive); the other ~87 (every slope, every weight, the EU gate cap, ORP) are product-decision-documented-but-unsupported or fully unknown. Final Decision Matrix: 2 rows "already adequate," rest split between NEEDS EVIDENCE / NEEDS PRODUCT DECISION / DOCUMENT ONLY. Zero rows marked "implement now." No code changed. **Final Trustworthiness Review**: direct verdict — "trustworthy as a compliance signal for a single country in isolation, not yet trustworthy for numeric cross-country comparison." New quantified finding: across 11 profiles run through this whole series, **Thailand scores highest or ties in 9/11 (82%)** — traced to its engine having the widest, least-evidenced bands, not confirmed regulatory reality. Flagged as the single highest-priority open decision: should the product ever frame "Thailand 90 vs Japan 85" as a meaningful comparison? Also newly precise: ORP's *parameter grade* is identical everywhere (confirmed), but final *composite* can differ slightly at extremes purely from weight-dilution arithmetic, not any country-specific ORP judgment — stated precisely to avoid a false "ORP got differentiated" impression. EU gate re-confirmed to collapse a real 16-point raw-composite spread (88/85/80) into an identical displayed 65 across three genuinely different chlorine readings. **Final synthesis round**: decomposed the 82%-Thailand-wins pattern case-by-case — of the 6 profiles Thailand wins alone (not tied), all 6 trace to unsupported wide TDS/Turbidity bands or DO not being scored, zero to confirmed standard difference; the 2 profiles with genuine standard-width differences (pH) are won by Japan and EU respectively, not Thailand. Final verdict: **YELLOW — safe as a country-specific compliance signal, BLOCKED for cross-country numeric ranking.** Also found a second, distinct semantic mismatch: the same score gets a different verdict adjective ("Excellent" vs "Good") depending on which engine produced it, since all 5 engines' tier cutoffs differ.
- [`COUNTRY_SCORE_REALISM_AUDIT.md`](COUNTRY_SCORE_REALISM_AUDIT.md) — 249-point parameter-isolation + threshold-crossing sweep across all 6 parameters × 5 countries, with a strict resolution-loss taxonomy (TRUE SAME RESULT / ROUNDING / AGGREGATION DILUTION / FLAT-GRADE LOSS / SCORING DESIGN DEFECT). Finds **one confirmed SCORING DESIGN DEFECT**: ORP's grading formula and band are byte-identical across all 5 country engines (re-verified with a 71-point sweep — zero differentiation possible for any ORP input, for any country, ever), with no cited standard for any of the 5 countries. No rounding or aggregation dilution found anywhere else in the sweep. No code changed — the ORP finding requires a product decision, not proposed here.
- [`COUNTRY_BENCHMARK_SEMANTIC_CONTRACT.md`](COUNTRY_BENCHMARK_SEMANTIC_CONTRACT.md) — **active product contract** (HEAD `02387730+`): Country Benchmark = country-specific compliance/benchmark signal, **not** universal quality, **not** cross-country ranking; per-engine semantics (Thailand DO exclusion by design, EU 65 = unsupported gate anchor not Directive score, ORP = shared operational band); **STOP — NO SAFE NUMERIC FIX** until evidence + product decisions. UI copy and semantic tests only — scoring math unchanged.
- [`SCORING_RESOLUTION_IMPLEMENTATION_REVIEW.md`](SCORING_RESOLUTION_IMPLEMENTATION_REVIEW.md) — the most implementation-focused pass: distinguishes resolution loss at the *parameter curve* (Chlorine's flat near-ideal zone) from resolution loss at *aggregation/rounding* (TDS/Turbidity — parameter grade genuinely moves, composite rounds it away), proven with a controlled adjacent-value test. Gap fixtures built only from existing limits (incl. the repo's own `LOCKED`/`POOR` test fixtures) prove real cross-country differentiation already exists where standards genuinely differ. Ends with a direct file-level list of exactly what would need to change if only the two stated goals were approved — nothing implemented.
- [`SCORING_RESOLUTION_REVIEW.md`](SCORING_RESOLUTION_REVIEW.md) — the capstone document: full current-state matrices for Quality *and* all 5 country engines (verified directly against every `score.js`, correcting an earlier oversimplification — four engines already grade TDS/Turbidity, not just WHO), a distilled one-row-per-parameter candidate design, and diagnostic scenarios A–E proving the review's central finding: aggregation dilution makes a real, evidence-backed cross-engine breakpoint (TDS 300 mg/L) invisible in every rounded composite score. Recommends exactly one change with real confidence (Turbidity center) and flags two pre-existing bugs; everything else is marked as a product decision, not a research gap.

## The one rule that matters most

```
A sample used to change a model parameter is calibration data, permanently,
for that model version. It can never later be described as validation or
holdout evidence — no matter how the test assertions around it are written.
```

See `docs/QUALITY_V3_MODEL_SPECIFICATION.md` (repo root `docs/`) for the
model's parameter-provenance table and known-gap documentation from the
prior boundary-hardening pass — this directory extends that work with the
evidence-partition discipline needed before any future recalibration.
